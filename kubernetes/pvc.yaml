apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trace-db-pvc
spec:
  accessModes:
    - ReadWriteMany  # Allows multiple pods to mount the volume
  resources:
    requests:
      storage: 2Gi  # Increased storage for database growth
  storageClassName: ""  # Use default storage class or specify your own
